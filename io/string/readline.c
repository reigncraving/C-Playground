#include <stdio.h>
#include <string.h>

int main(void) {
    char *buffer = NULL;
    size_t buffer_size = 32;
    size_t character; 

    buffer = (char *)malloc(buffer_size * sizeof(char));
    return 0;
}